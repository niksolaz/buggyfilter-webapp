/**
 * Tema Nuxt UI mappato sui token del design system BuggyFilter:
 * brand rosso (come la landing), neutri slate.
 */
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'orange',
      neutral: 'slate',
      success: 'green',
      warning: 'amber',
      info: 'blue',
      error: 'red',
    },
  },
})
